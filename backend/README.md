📍 Backend - Localiza Fácil
Este diretório contém a API em Node.js/Express.
O backend já está hospedado no Render, mas caso queira rodar localmente siga os passos abaixo:

⚙️ Como rodar localmente
- Entre na pasta do backend:
cd backend
- Instale as dependências:
npm install
- Inicie o servidor:
npm start
- A API ficará disponível em:
http://localhost:3000

